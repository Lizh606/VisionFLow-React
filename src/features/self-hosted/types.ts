
// Add any specific Self Hosted types here if needed in the future
// Currently using inferred types in components, but prepared for strict typing
export interface Device {
  id: string;
  name: string;
  deviceId: string;
  status: string;
}
